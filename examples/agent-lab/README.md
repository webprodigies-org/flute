# Agent lab

A clean shadcn dashboard host for testing the installed Flute package. Run `npm run setup:agent-lab` from the repository root to install it, then `npm --prefix examples/agent-lab run dev`. In this directory, `npx flute --help` is the package entry point.

The existing dashboard components and data are the source of truth for this host UI.
